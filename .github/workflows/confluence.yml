name: Strategy_Confluence

on:
  push:
    paths:
      - 'confluence_hunter.py'
      - '.github/workflows/confluence.yml'
      - 'results/**'
  schedule:
    # 北京时间 16:45 运行（确保 16 战法引擎已跑完）
    - cron: '45 08 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run_analysis:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install pandas tabulate

      - name: Run Analysis
        # 即使部分 CSV 为空报错，脚本也会继续运行并产出报告
        run: python confluence_hunter.py

      - name: Commit and Push All Changes
        run: |
          git config --local user.email "bot@github.com"
          git config --local user.name "ConfluenceBot"
          
          # 确保关键目录存在，防止 add 时报错
          mkdir -p results reports history
          
          # 1. 将所有新产生的结果和报告加入暂存区
          # 使用强制添加路径的方式，确保 untracked files (如新生成的 reports/) 被追踪
          git add results/
          git add reports/
          
          # 2. 如果你有特殊的本地文件需要同步，可以取消下面的注释
          # git add 显示方式.md || true

          # 3. 检查是否有任何变动 (包含新增文件)
          if [ -n "$(git status --porcelain)" ]; then
            echo "检测到新生成的报告或数据，准备提交..."
            git commit -m "Auto: 16战法共振分析及复盘报告 $(date +'%Y-%m-%d')"
            git pull --rebase origin main
            git push origin main
            echo "✅ 变动已成功推送至仓库。"
          else
            echo "📭 未发现任何变动，跳过提交步骤。"
          fi
